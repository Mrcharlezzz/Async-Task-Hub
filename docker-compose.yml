services:
  redis:
    image: redis:7-alpine
    container_name: asynctaskhub_redis
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 2s
      retries: 10
    networks: [asynctaskhub_net]

  api:
    build:
      context: .
      target: api
    container_name: asynctaskhub_api
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    command: >
      uvicorn src.app.presentation.main:app
      --host 0.0.0.0 --port 8000
    restart: unless-stopped
    volumes:
      - .:/app
      - naive_data:/data
    networks: [asynctaskhub_net]

  db:
    image: postgres:16
    env_file:
      - .env
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks: [asynctaskhub_net]

  worker:
    build:
      context: .
      target: worker
    container_name: asynctaskhub_worker
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
      db:
        condition: service_healthy
    command: >
      python -m src.app.worker.main
    restart: unless-stopped
    volumes:
      - .:/app
    networks: [asynctaskhub_net]

  naive_demo_worker:
    build:
      context: .
      target: runtime
    container_name: asynctaskhub_naive_demo_worker
    command: >
      python -m src.naive_worker.compute_pi.worker
    volumes:
      - .:/app
      - naive_data:/data
    networks: [asynctaskhub_net]

  naive_demo_doc_worker:
    build:
      context: .
      target: runtime
    container_name: asynctaskhub_naive_demo_doc_worker
    command: >
      python -m src.naive_worker.document_analysis.worker
    volumes:
      - .:/app
      - naive_data:/data
    networks: [asynctaskhub_net]

  demo:
    image: nginx:alpine
    container_name: asynctaskhub_demo
    depends_on:
      - api
    ports:
      - "8080:80"
    volumes:
      - ./demo:/usr/share/nginx/html:ro
      - ./demo/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks: [asynctaskhub_net]

  tests:
    profiles: ["test"]
    build:
      context: .
      target: test
    container_name: asynctaskhub_tests
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
      db:
        condition: service_started
    command: >
      pytest -rs
    volumes:
      - .:/app
    networks: [asynctaskhub_net]

networks:
  asynctaskhub_net:
    driver: bridge

volumes:
  pgdata:
  naive_data:
