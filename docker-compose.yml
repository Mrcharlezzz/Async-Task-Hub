version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: posttager_redis
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 2s
      retries: 10
    networks: [posttager_net]

  api:
    build:
      context: .
      target: api
    container_name: posttager_api
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "8000:8000"
    command: >
      uvicorn src.app.presentation.main:app
      --host 0.0.0.0 --port 8000
    restart: unless-stopped
    networks: [posttager_net]

  db:
    image: postgres:16
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

  worker:
    build:
      context: .
      target: worker
    container_name: posttager_worker
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
    command: >
      celery -A src.app.infrastructure.celery.app:celery_app
      worker -l ${LOG_LEVEL:-INFO} --concurrency=1
    restart: unless-stopped
    networks: [posttager_net]

networks:
  posttager_net:
    driver: bridge
